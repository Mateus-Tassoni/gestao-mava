# Opcional: so use se quiser outra senha que nao a de dev (gestao123)
# copy .env.example .env

# PostgreSQL (Docker usa valores fixos em dev; aqui so se quiser override)
POSTGRES_DB=gestaodb
POSTGRES_USER=gestao
POSTGRES_PASSWORD=gestao123
